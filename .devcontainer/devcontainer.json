{
	"name": "Lucidity",
	"workspaceFolder" : "/workspace",
	"workspaceMount": "source=${localWorkspaceFolder},target=/workspace/Terraform-Pipelines,type=bind,consistency=delegated",	
	"build": {
		"dockerfile": "Dockerfile",
		"args": { "VARIANT": "focal" }
	},

	"settings": { 
		"terminal.integrated.shell.linux": "/bin/bash"
	},
  	"extensions": ["HashiCorp.terraform"],
	"remoteUser": "vscode"
}